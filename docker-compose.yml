services:
  web:
    build: .
    ports:
      - "3001:3000"
    volumes:
      - ./prisma/sqlite:/app/prisma/sqlite
      - ./secrets:/app/secrets
    environment:
      - DATABASE_URL=file:./sqlite/dev.db
    restart: unless-stopped
