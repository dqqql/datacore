---
角色: 项目总控
任务ID: 20260313-1325-CTRL
触发阶段: 变更评估
依赖输入: 20260313-1235-CTRL, 用户确认“服务器初版已部署并希望收口 Docker 更新流程”
输出时间: 2026-03-13 13:25
---

## 本轮整合结论
- 调用角色: 项目总控
- 核心共识: 本地代码已通过 `npm run build`，容器配置已调整为以 `docker-compose.yml` 为主入口，后续目标流程收敛为“服务器 `git pull` 后执行 `docker compose up -d --build`”。
- 冲突处理: 保留运行时 `prisma db push`，不把数据库结构更新放到镜像构建阶段，避免构建过程依赖宿主机数据卷和运行态目录。

## 待确认项
- P0：服务器当前是否仍有历史容器（如 `datacore-ui-test`）在运行，避免和新的 compose 容器命名或端口冲突。
- P1：服务器实际对外端口是否继续沿用 `3001`，还是已通过反向代理映射到域名。

## 风险与约束区
- 本机 Docker 守护进程当前不可用，因此已完成 `docker compose config` 静态校验，但未能在本地执行真实镜像构建。
- `docker-compose.yml` 目前默认挂载 `./prisma/sqlite` 与 `./secrets`，服务器仓库目录必须保留这两个目录，且 `.env` 需存在。

## 依赖关系
- 依赖 项目总控 的 20260313-1235-CTRL：沿用统一版本号 `v0.2.7` 的当前有效口径。
- 阻塞 服务器发布切换：在确认服务器当前容器运行状态前，不建议直接删除旧容器或强切 compose。

## 变更说明
- 变更类型: 技术调整
- 影响范围: `Dockerfile`、`docker-compose.yml`、`.dockerignore`、`.env.example`
- 失效内容: “首次部署后继续手工 `docker run` 更新镜像”的旧操作路径
- 保留内容: SQLite 数据卷挂载、`secrets` 挂载、运行时自动 `prisma db push` 的首版策略继续有效
