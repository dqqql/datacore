---
角色: 页面交互设计/前端开发
任务ID: 20260313-1123-UINAV
触发阶段: 正式上线准备
依赖输入: 无
输出时间: 2026-03-13 11:32
---

## 2.1 结论区
- **统一身份出口落实**：在没有引入额外登出页面的前提下，直接重构了 `/login` 界面逻辑。现在如果在已登录状态下访问“登录”页面，系统将不再自动判定为无效跳转，而是会渲染一个带有登录态卡片（展示当前身份账号和头像首字母）的「账号控制面板」。
- **下发离线通道**：在该账号面板内，正式封装下放了 `LogoutButton`，使得无论是管理员还是普通账户都可以通过点击该页面的“退出登录并切换账号”完成会话注销，并立刻切换回普通登录表单；同时提供了“返回控制台”的按钮，方便从误触快速退出。
- **左侧导航收敛**：原本的 `/` (总览) 作为空占位导流页面已经被彻底从左侧公会导航组删除，减少毫无意义且导致困惑的跳转。

## 2.2 待确认区
- 无。该调整无需二次配置，已跑通并提交。

## 2.3 风险与约束区
- **根目录设定保留**：对于玩家直接输入根域名 `http://localhost:3000/` 的行为，此处的隐性跳转向 `/dashboard` 的底层路由拦截被策略性保留，这也是最符合 Web App 预期的访问习惯，只去除了在已有侧边栏导航里显式的多余按键。
