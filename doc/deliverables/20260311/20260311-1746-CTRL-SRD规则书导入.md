---
角色: 项目总控
任务ID: 20260311-1746-CTRL
触发阶段: 执行准备
依赖输入: 20260311-1544-CTRL, 用户新增 srd.md
输出时间: 2026-03-11 17:46
---

## 结论区
- 已将项目根目录下的 `srd.md` 接入为规则书物品的数据源，并完成实际入库。
- 当前实现采用“源文件驱动 + 轻解析”路线：
  - `srd.md` 继续作为原始数据源
  - 应用侧解析后写入 `rulebook` 商店
  - 不把大批基础物品硬编码在 TS 常量里
- 已完成结果：
  - 新增 `priceLabel` 字段，用于保留 `sp/cp/gp` 原始价格文本
  - 新增 `src/lib/srd-parser.ts` 负责轻量解析
  - `ensureDefaultShops()` 现可自动导入并更新 SRD 条目
  - 规则书页面改为读取真实数据库目录，不再是静态占位
  - 当前已确认入库的 SRD 条目数：149
- 验证已完成：
  - `npm run db:push`
  - `npm run lint`
  - `npm run build`
  - 抽样确认：护甲、武器分类与原始价格文本已正确入库

## 待确认区
- P1：是否需要把规则书目录页继续补成“按分类折叠 / 搜索”版本，方便后续浏览大批量条目。
- P1：是否需要给管理员后台补 `priceLabel` 的只读显示，避免在后台维护时只看到整数价格字段。

## 风险与约束区
- 当前解析依赖 `srd.md` 的现有文本结构；若源文件格式大幅变化，需要同步调整解析器。
- `price` 字段仍然保留为整数，供现有模型兼容；`sp/cp` 等原始价格通过 `priceLabel` 展示，不参与直接购买换算。
- 本轮仅完成规则书目录导入与展示，不把规则书页改造成可购买商店。

## 依赖关系
- 依赖 项目总控 的 20260311-1544-CTRL：在主流程与并发保护收口后，继续补规则书目录数据。
- 阻塞 手动验收：无阻塞；用户现在可直接打开规则书页核对导入结果。

## 变更说明
- 变更类型: 范围补齐
- 影响范围: `prisma/schema.prisma`、`src/lib/bootstrap-shops.ts`、`src/lib/srd-parser.ts`、`src/app/shops/rulebook/page.tsx`、本地 `prisma/dev.db`
- 失效内容: 规则书页仅为静态预览、规则书物品未实际导入的旧结论已失效
- 保留内容: 公会商店、荣誉商店、玩家交易和后台维护主链路保持有效
